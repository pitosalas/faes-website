# Feature description for feature F01
## F01 — Project scaffold and content directory structure
**Priority**: High
**Done:** yes
**Tests Written:** yes
**Test Passing:** yes
**Description**: Set up the project directory structure including the `content/` directory for source material, a `site/` output directory for generated HTML, and the Python package with `pyproject.toml` so the generator can be launched with `uv run`. Also initialize the git repository with appropriate `.gitignore`.
