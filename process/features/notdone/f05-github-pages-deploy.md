# Feature description for feature F05
## F05 — GitHub Pages deployment via GitHub Actions
**Priority**: Medium
**Done:** no
**Tests Written:** no
**Test Passing:** no
**Description**: Configure a GitHub Actions workflow that triggers on every push to main. The workflow runs the Python site generator and deploys the `site/` output directory to GitHub Pages. The live site should be accessible at the repository's GitHub Pages URL immediately after each push.
